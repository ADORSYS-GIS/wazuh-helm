<!-- Modify it at your will. -->
<decoder name="owasp_json">
    <prematch>^{\s*"@programName"\s*:\s*"ZAP"</prematch>
    <plugin_decoder>JSON_Decoder</plugin_decoder>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"riskcode\".*?:.*?\"(.*?)\"</regex>
    <order>riskcode</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"desc\".*?:.*?\"(.*?)\"</regex>
    <order>desc</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"count\".*?:.*?\"(.*?)\"</regex>
    <order>count</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"solution\".*?:.*?\"(.*?)\"</regex>
    <order>solution</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"sourceid\".*?:.*?\"(.*?)\"</regex>
    <order>sourceid</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"pluginid\".*?:.*?\"(.*?)\"</regex>
    <order>pluginid</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"alertRef\".*?:.*?\"(.*?)\"</regex>
    <order>alertRef</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"alert\".*?:.*?\"(.*?)\"</regex>
    <order>alert</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"@name\".*?:.*?\"(.*?)\"</regex>
    <order>@name</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"@version\".*?:.*?\"(.*?)\"</regex>
    <order>@version</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"@generated\".*?:.*?\"(.*?)\"</regex>
    <order>@generated</order>
</decoder>

<decoder name="owasp_json">
    <parent>owasp_json</parent>
    <regex type="pcre2">\"@programName\".*?:.*?\"(.*?)\"</regex>
    <order>@programName</order>
</decoder>
