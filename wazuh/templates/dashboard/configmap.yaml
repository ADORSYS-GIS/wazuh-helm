apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" $ }}-dashboard-conf
data:
   {{- $f := include "common.tplvalues.render" (dict "value" ($.Files.Get "files/configs/opensearch_dashboards.yml") "context" $) }}
  "opensearch_dashboards.yml": {{ $f | quote }}