{{ with .Values.dashboard.auth -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "common.names.fullname" $ }}-dashboard-cred
type: kubernetes.io/basic-auth
stringData:
  DASHBOARD_USERNAME: '{{ .username }}'
  DASHBOARD_PASSWORD: '{{ .password }}'
{{- end }}