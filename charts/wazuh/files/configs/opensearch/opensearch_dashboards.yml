server.host: 0.0.0.0
server.port: 5601
opensearch.hosts: https://{{ include "common.names.fullname" $ }}-indexer-api:9200
opensearch.ssl.verificationMode: none
opensearch.requestHeadersAllowlist: ["Authorization", "securitytenant"]
opensearch_security.multitenancy.enabled: true
{{- if and $.Values.indexer.multiTenancy $.Values.indexer.multiTenancy.enabled }}
opensearch_security.multitenancy.tenants.preferred: ["Global"{{ range $tenantName, $tenant := $.Values.indexer.multiTenancy.tenants }}, "{{ $tenantName }}"{{ end }}, "Private"]
{{- else }}
opensearch_security.multitenancy.tenants.preferred: ["Global", "Private"]
{{- end }}
opensearch_security.readonly_mode.roles: ["kibana_read_only"]
server.ssl.enabled: true
server.ssl.key: "/usr/share/wazuh-dashboard/certs/dashboard-key.pem"
server.ssl.certificate: "/usr/share/wazuh-dashboard/certs/dashboard.pem"
opensearch.ssl.certificateAuthorities: ["/usr/share/wazuh-dashboard/certs/root-ca.pem"]
uiSettings.overrides.defaultRoute: /app/wz-home

opensearch_security.auth.unauthenticated_routes: ['/api/status']

# Session expiration settings
opensearch_security.cookie.ttl: 900000
opensearch_security.session.ttl: 900000
opensearch_security.session.keepalive: true
  
{{ with $.Values.indexer.keycloak -}}
{{- if and .enabled (not (contains "<keycloak-url>" .openid_connect_url)) -}}
{{- /* Check if we have direct credentials or existingSecret */ -}}
{{- $hasDirectCreds := and .client_id .client_secret -}}
{{- $hasExistingSecret := .existingSecret -}}
{{- if or $hasDirectCreds $hasExistingSecret -}}

opensearch_security.auth.type: "openid"
opensearch_security.openid.connect_url: '{{ include "common.tplvalues.render" (dict "value" .openid_connect_url "context" $) }}'
{{- if $hasDirectCreds }}
opensearch_security.openid.client_id: '{{ include "common.tplvalues.render" (dict "value" .client_id "context" $) }}'
opensearch_security.openid.client_secret: '{{ include "common.tplvalues.render" (dict "value" .client_secret "context" $) }}'
{{- else }}
# Credentials loaded from environment variables (existingSecret: {{ .existingSecret }})
opensearch_security.openid.client_id: '${OIDC_CLIENT_ID}'
opensearch_security.openid.client_secret: '${OIDC_CLIENT_SECRET}'
{{- end }}
{{ with .scope -}}
opensearch_security.openid.scope: '{{ include "common.tplvalues.render" (dict "value" . "context" $) }}'
{{- end }}
{{ with .base_redirect_url -}}
opensearch_security.openid.base_redirect_url: '{{ include "common.tplvalues.render" (dict "value" . "context" $) }}'
{{- end }}

{{- else -}}
# Basic authentication (Keycloak credentials not configured)
opensearch_security.auth.type: "basicauth"
{{- end -}}
{{- else -}}
# Basic authentication (Keycloak not enabled or invalid URL)
opensearch_security.auth.type: "basicauth"
{{- end -}}
{{- end }}
  
{{ with $.Values.dashboard.branding -}}
opensearchDashboards.branding:
  {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 2 }}
{{- end }}